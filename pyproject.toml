[project]
name = "hands-on"
version = "0.1.0"
description = "Manual NumPy-only ML algorithms from scratch"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26",
]

[tool.uv]
package = false
